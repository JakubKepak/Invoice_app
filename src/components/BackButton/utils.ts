import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  backButtonWrapper: {
    display: "flex",
    alignItems: "center",
    position: "absolute",
    top: 0,
    left: "10px",
  },
}));
